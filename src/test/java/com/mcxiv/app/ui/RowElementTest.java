package com.mcxiv.app.ui;

import com.kotcrab.vis.ui.VisUI;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class RowElementTest {

    // TODO: Error VisUI not loaded

   /* @Test
    void fromStringTest() {
        RowElement ele = RowElement.fromString("{this is a link==true}");
        assertEquals(ele.getLink(), "this is a link");
        assertTrue(ele.getUpdatePreference());


        ele = RowElement.fromString("{872q36vcr8t35rb6*&%^#$)@^B6==false}");
        assertEquals(ele.getLink(), "872q36vcr8t35rb6*&%^#$)@^B6");
        assertFalse(ele.getUpdatePreference());

    }*/
}